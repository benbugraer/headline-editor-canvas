import React from "react";
import { Canvas } from "fabric";
interface TopBarProps {
    canvas: Canvas | null;
}
export default function TopBar({ canvas }: TopBarProps): React.JSX.Element;
export {};
